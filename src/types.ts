export interface Project {
  title: string;
  description: string;
  thumbnail: string;
  techStack: string[];
  demoUrl: string;
  githubUrl: string;
}

export interface SocialLinks {
  platform: string;
  url: string;
  icon: string;
}